<!-- pages/user/center/center.wxml -->
<view class="container">
  <!-- 用户信息头部 -->
  <view class="user-header">
    <view class="user-info" wx:if="{{isLoggedIn}}">
      <image class="avatar" src="{{userInfo.avatarUrl || '/images/avatar-default.png'}}"></image>
      <view class="user-detail">
        <view class="nickname">{{userInfo.nickName || '用户'}}</view>
        <view class="user-tag" wx:if="{{isAdmin}}">
          <text class="tag tag-primary">管理员</text>
        </view>
      </view>
    </view>
    
    <view class="login-prompt" wx:else>
      <image class="avatar avatar-default" src="/images/avatar-default.png"></image>
      <button class="login-btn" bindtap="getUserProfile">点击登录</button>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-section card">
    <view class="menu-title">我的服务</view>
    <view 
      class="menu-item"
      wx:for="{{menuList}}"
      wx:key="id"
      bindtap="onMenuTap"
      data-url="{{item.url}}"
    >
      <view class="menu-icon">{{item.icon}}</view>
      <view class="menu-text">{{item.title}}</view>
      <view class="menu-arrow">></view>
    </view>
  </view>

  <!-- 管理员菜单 -->
  <view class="menu-section card" wx:if="{{isAdmin}}">
    <view class="menu-title">管理功能</view>
    <view 
      class="menu-item"
      wx:for="{{adminMenuList}}"
      wx:key="id"
      bindtap="onMenuTap"
      data-url="{{item.url}}"
    >
      <view class="menu-icon">{{item.icon}}</view>
      <view class="menu-text">{{item.title}}</view>
      <view class="menu-arrow">></view>
    </view>
  </view>

  <!-- 其他操作 -->
  <view class="menu-section card">
    <view class="menu-item" bindtap="onAbout">
      <view class="menu-icon">ℹ️</view>
      <view class="menu-text">关于我们</view>
      <view class="menu-arrow">></view>
    </view>
    
    <view class="menu-item" wx:if="{{isLoggedIn}}" bindtap="onLogout">
      <view class="menu-icon">🚪</view>
      <view class="menu-text text-error">退出登录</view>
      <view class="menu-arrow">></view>
    </view>
  </view>
</view>
