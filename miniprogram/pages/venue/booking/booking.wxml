<!-- pages/venue/booking/booking.wxml -->
<view class="container">
  <!-- 预约信息 -->
  <view class="booking-info card">
    <view class="info-title">预约信息</view>
    <view class="info-item">
      <text class="info-label">场馆名称</text>
      <text class="info-value">{{venueName}}</text>
    </view>
    <view class="info-item">
      <text class="info-label">场馆类型</text>
      <text class="info-value">{{venueTypeText}}</text>
    </view>
    <view class="info-item">
      <text class="info-label">预约日期</text>
      <text class="info-value">{{date}}</text>
    </view>
    <view class="info-item">
      <text class="info-label">预约时段</text>
      <text class="info-value">{{startTime}} - {{endTime}}</text>
    </view>
    <view class="info-item">
      <text class="info-label">费用</text>
      <text class="info-value price">¥{{price}}</text>
    </view>
  </view>

  <!-- 预约人信息 -->
  <view class="user-info card">
    <view class="info-title">预约人信息</view>
    <view class="form-group">
      <view class="form-label">
        <text class="required">*</text>
        姓名
      </view>
      <input 
        class="form-input" 
        placeholder="请输入预约人姓名" 
        value="{{userName}}"
        bindinput="onNameInput"
      />
    </view>
    <view class="form-group">
      <view class="form-label">
        <text class="required">*</text>
        联系电话
      </view>
      <input 
        class="form-input" 
        type="number"
        maxlength="11"
        placeholder="请输入联系电话" 
        value="{{userPhone}}"
        bindinput="onPhoneInput"
      />
    </view>
    <view class="form-group">
      <view class="form-label">备注</view>
      <textarea 
        class="form-textarea" 
        placeholder="如有特殊需求请备注"
        value="{{remark}}"
        bindinput="onRemarkInput"
        maxlength="200"
      ></textarea>
    </view>
  </view>

  <!-- 提示信息 -->
  <view class="tips card">
    <view class="tips-title">温馨提示</view>
    <view class="tips-content">
      <view class="tip-item">1. 请在预约时间前15分钟到达场馆</view>
      <view class="tip-item">2. 如需取消预约，请提前2小时操作</view>
      <view class="tip-item">3. 首次预约免费，后续预约需现场付费</view>
    </view>
  </view>

  <!-- 底部提交按钮 -->
  <view class="bottom-bar safe-area-bottom">
    <view class="price-info">
      <text class="price-label">合计：</text>
      <text class="price-total">¥{{price}}</text>
    </view>
    <button 
      class="submit-btn" 
      bindtap="submitBooking"
      loading="{{submitting}}"
      disabled="{{submitting}}"
    >
      确认预约
    </button>
  </view>
</view>
